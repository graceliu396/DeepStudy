# from fastapi import APIRouter
# from pydantic import BaseModel

# router = APIRouter()

# @router.post("/auth/register", response_model=UserResponse)
# async def register(user: UserCreate):
#     # 检查第三方ID是否已存在
#     # 创建用户文档存储到CosmosDB
#     return {"user_id": "123", "grade": "G7"}